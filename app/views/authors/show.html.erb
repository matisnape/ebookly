<h1><%= @author.display_name %></h1>

<table>
  <tbody id="authors">
    <%= render @author %>
  </tbody>
</table>

<h2>Books from this author:</h2>
<ol>
  <% @author.books.each do |book| %>
  <li><%= book.title %> (<%= link_to book.shop_name, book.shop %>)</li>
  <% end %>
</ol>

<br />
