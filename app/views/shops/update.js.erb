<% if @shop.present? && flash[:error].blank? %>
  $('tr#<%= @shop.id %>').html("<%= escape_javascript(render @shop) %>");
  $('.alert').html("<%= escape_javascript(render 'flash_messages') %>");
  $('.alert').delay(5000).fadeOut("slow", function () { $(this).remove();})
<% end %>
