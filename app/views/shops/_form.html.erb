<div>
  <%= render 'shared/error_messages', object: @shop %>

  <%= form_with model: @shop, local: true do |form| %>
    <%= form.label :name %>
    <%= form.text_field :name %>

    <%= form.submit "Submit" %>
  <% end %>
</div>

