<tr id="<%= shop.id %>">
  <td><% @shops ? @shops.index(shop) + 1 : '' %></td>
  <td><%= shop.name %></td>
  <td><%= link_to "Edit", shop, method: :edit %></td>
  <td><%= link_to "Delete", shop, remote: true, method: :delete, data: { confirm: "Are you sure?" } %></td>
</tr>
